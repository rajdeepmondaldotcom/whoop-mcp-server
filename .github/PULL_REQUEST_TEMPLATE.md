**What does this change?**

**Why?**

**Checklist**
- [ ] `pytest` passes (includes a test that fails without this change)
- [ ] `ruff check .` passes
- [ ] No server code path writes to stdout
- [ ] Tool docstrings updated if tool behavior changed
